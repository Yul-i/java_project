<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="people">
	<insert id="create" parameterType="peopleVO">
		insert into PEOPLE (MEMID, MEMPW, MEMNAME, BIRTHDAY, GENDER, EMAIL, TEL, ADDRESS, MEMLEVEL) values (#{memId}, #{memPw}, #{memName}, #{birthday}, #{gender}, #{email}, #{tel}, #{address}, #{memLevel})
	</insert>
	<select id="one" parameterType="peopleVO" resultType="peopleVO">
		select * from PEOPLE where MEMNAME = #{memName}
	</select>
</mapper>